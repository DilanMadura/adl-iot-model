import { DeviceContextParameter } from './device-context-parameter';

describe('DeviceContextParameter', () => {
  it('should create an instance', () => {
    expect(new DeviceContextParameter()).toBeTruthy();
  });
});
