import { DeviceConfiguration } from './device-configuration';

describe('DeviceConfiguration', () => {
  it('should create an instance', () => {
    expect(new DeviceConfiguration()).toBeTruthy();
  });
});
