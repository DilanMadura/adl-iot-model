import { UserActionTypePermissions } from './user-action-type-permissions';

describe('UserActionTypePermissions', () => {
  it('should create an instance', () => {
    expect(new UserActionTypePermissions()).toBeTruthy();
  });
});
