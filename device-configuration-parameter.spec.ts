import { DeviceConfigurationParameter } from './device-configuration-parameter';

describe('DeviceConfigurationParameter', () => {
  it('should create an instance', () => {
    expect(new DeviceConfigurationParameter()).toBeTruthy();
  });
});
