import { DeviceDefinitionContext } from './device-definition-context';

describe('DeviceDefinitionContext', () => {
  it('should create an instance', () => {
    expect(new DeviceDefinitionContext()).toBeTruthy();
  });
});
