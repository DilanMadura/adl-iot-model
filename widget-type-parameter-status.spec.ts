import { WidgetTypeParameterStatus } from './widget-type-parameter-status';

describe('WidgetTypeParameterStatus', () => {
  it('should create an instance', () => {
    expect(new WidgetTypeParameterStatus()).toBeTruthy();
  });
});
