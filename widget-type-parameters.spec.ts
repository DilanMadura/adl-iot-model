import { WidgetTypeParameters } from './widget-type-parameters';

describe('WidgetTypeParameters', () => {
  it('should create an instance', () => {
    expect(new WidgetTypeParameters()).toBeTruthy();
  });
});
