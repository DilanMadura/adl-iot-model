import { AlertTriggeredHistory } from './alert-triggered-history';

describe('AlertTriggeredHistory', () => {
  it('should create an instance', () => {
    expect(new AlertTriggeredHistory()).toBeTruthy();
  });
});
